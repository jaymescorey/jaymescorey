# Supabase — get from supabase.com → project → Settings → API
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Stripe — get from Stripe dashboard → Developers → Webhooks → signing secret
STRIPE_WEBHOOK_SECRET=

# MailerLite — account ID is hardcoded in BaseLayout.astro (2182045), no env var needed currently

# Note: No .env file is committed. Copy this file to .env and fill in values locally.
# Add the same keys to Cloudflare Pages → Settings → Environment Variables for production.
